# config.py

DB_CONFIG = {
    'host': 'localhost',
    'user': 'root',          # default XAMPP username
    'password': '',          # default password is blank (unless you changed it)
    'database': 'tv_store',  # the name of your MySQL database
    'port': 3306             # default MySQL port in XAMPP (unless changed to 3307)
}
